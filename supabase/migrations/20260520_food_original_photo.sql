-- ============================================================================
--  AI 음식 추가 — 다중 음식 선택 + 원본/자른 사진 같이 관리
--
--  같은 사진에서 추출된 여러 음식이 동일 original_photo_url을 공유.
--  photo_url(기존)은 음식별 cropped 200x200 썸네일.
-- ============================================================================

ALTER TABLE public.foods
  -- 분석에 사용된 원본 사진 (Storage path). 여러 foods가 같은 원본을 공유 가능.
  ADD COLUMN IF NOT EXISTS original_photo_url text;
